spring:
  profiles:
    active: "oauth"

  session:
    store-type: jdbc

  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: none #!!!!! 이거때문에 다시 만드는듯..

#  datasource:
#    url: jdbc:mysql://localhost:3306/book_manager
#    username: root
#    password:

server:
  port: 8070

